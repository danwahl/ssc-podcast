---
---
<?xml version="1.0" encoding="UTF-8"?>
<rss version="2.0" xmlns:googleplay="http://www.google.com/schemas/play-podcasts/1.0"
     xmlns:itunes="http://www.itunes.com/dtds/podcast-1.0.dtd">
  <channel>
    <title>{{ site.title }}</title>
    <googleplay:author>{{ site.author }}</googleplay:author>
    <description>{{ site.description }}</description>
    <googleplay:image href="{{ site.url }}{{ site.baseurl }}/{{ site.image }}"/>
    <language>en-us</language>
    <link>{{ site.url }}/</link>
    {% for post in site.posts %}
    <item>
      <title>{{ post.title }}</title>
      <description>{{ post.description }}</description>
      <pubDate>{{ post.date | date: "%a, %d %b %Y %H:%M:%S PST" }}</pubDate>
      <enclosure url="{{ site.url }}{{ site.baseurl }}/podcasts/{{ post.guid }}.mp3"
                 type="audio/mpeg" length="{{ post.length }}"/>
      <itunes:duration>{{ post.duration }}</itunes:duration>
      <guid isPermaLink="false">{{ post.guid }}</guid>
    </item>
    {% endfor %}
  </channel>
</rss>
